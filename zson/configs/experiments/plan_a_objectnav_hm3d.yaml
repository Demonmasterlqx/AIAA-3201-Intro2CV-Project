VERBOSE: True

BASE_TASK_CONFIG_PATH: "configs/tasks/plan_a_objectnav_hm3d.yaml"

TRAINER_NAME: "zson-ppo"
ENV_NAME: "SimpleRLEnv"
SENSORS: ["RGB_SENSOR", "DEPTH_SENSOR"]

VIDEO_OPTION: []
VIDEO_DIR: "outputs/plan_a/video"
TENSORBOARD_DIR: "outputs/plan_a/tensorboard"
EVAL_CKPT_PATH_DIR: "data/checkpoints"
CHECKPOINT_FOLDER: "data/checkpoints"
LOG_FILE: "outputs/plan_a/plan_a.log"

NUM_ENVIRONMENTS: 1
LOG_INTERVAL: 1
NUM_CHECKPOINTS: 1
NUM_UPDATES: -1
TOTAL_NUM_STEPS: 1.0

FORCE_TORCH_SINGLE_THREADED: True

EVAL:
  SPLIT: "val"

RL:
  REWARD_MEASURE: "distance_to_goal"
  SUCCESS_MEASURE: "success"

PLAN_A:
  DATASET: "hm3d"
  SPLIT: "val"
  OUTPUT_ROOT: "outputs/plan_a"
