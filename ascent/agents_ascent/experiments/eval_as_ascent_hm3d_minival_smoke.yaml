defaults:
  - eval_as_ascent_hm3d_smoke
  - _self_

habitat:
  dataset:
    split: val_mini
    data_path: data/datasets/objectnav/hm3d/v1/{split}/{split}.json.gz
  simulator:
    scene_dataset: data/scene_datasets/hm3d/val/hm3d_annotated_minival_basis.scene_dataset_config.json

habitat_baselines:
  tensorboard_dir: "debug/tb_agents_ascent_naive_hm3d_minival_smoke"
  video_dir: "debug/video_agents_ascent_naive_hm3d_minival_smoke"
  eval:
    split: "val_mini"
