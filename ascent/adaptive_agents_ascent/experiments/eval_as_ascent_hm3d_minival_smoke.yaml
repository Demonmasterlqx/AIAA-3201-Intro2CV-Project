defaults:
  - eval_as_ascent_hm3d_smoke
  - _self_

habitat:
  dataset:
    split: val_mini
    data_path: data/datasets/objectnav/hm3d/v1/{split}/{split}.json.gz
  simulator:
    scene_dataset: data/scene_datasets/hm3d/val/hm3d_annotated_minival_basis.scene_dataset_config.json

habitat_baselines:
  tensorboard_dir: "results/as_ascent/hm3d/minival_smoke/tensorboard"
  video_dir: "results/as_ascent/hm3d/minival_smoke/videos"
  eval:
    split: "val_mini"
